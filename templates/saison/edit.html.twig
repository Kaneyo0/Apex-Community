{% extends 'base.html.twig' %}

{% block title %}Editer Saison - {% endblock %}

{% block body %}
    <h1>Editer Saison</h1>

    {{ include('saison/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    {{ include('saison/_delete_form.html.twig') }}
    <br>
    <a href="{{ path('saison_index') }}">Retour à la liste</a>

{% endblock %}
